// idl/hello/hello.proto
syntax = "proto3";

package hello;

option go_package = "frontend/home";

import "api.proto";

message Empty {}

service HomeService {
  rpc Home(Empty) returns(Empty) {
    option (api.get) = "/";
  }
}
